---
import { getCollection } from "astro:content";
import Breadcrumbs from "../../components/parts/Breadcrumbs.astro";
import BaseLayout from "../../layouts/BaseLayout.astro";
import PageTitle from "../../components/parts/PageTitle.astro";

export const getStaticPaths = async () => {
  const works = await getCollection("works");
  return works.map((e) => ({
    params: { slug: e.slug },
    props: { page: e },
  }));
};

const { page } = Astro.props;
const { Content } = await page.render();
---

<BaseLayout
  title={page.data.title}
  thumbnailPath={`/works/${page.slug}.png`}
  description={page.data.description}
>
  <Breadcrumbs
    items={[
      {
        title: "Home",
        path: "/",
      },
      {
        title: "Works",
        path: "/works",
      },
    ]}
  />
  <PageTitle title={page.data.title} />
  <p>
    <img src={`/works/${page.slug}.jpg`} />
  </p>
  <Content />
</BaseLayout>
